<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.min.edu.model.mapper.SignupMapper">

    <insert id="insertUser">

        INSERT INTO users (
            user_id,
            email,
            password,
            role,
            created_at
        )

        VALUES (
            #{userId},
            #{email},
            #{password},
            #{role},
            SYSDATE
        )

    </insert>


    <insert id="insertProfile">

        INSERT INTO profile (
            profile_id,
            user_id,
            username,
            rank,
            total_points
        )

        VALUES (
            #{profileId},
            #{userId},
            #{username},
            '병아리',
            0
        )

    </insert>

</mapper>